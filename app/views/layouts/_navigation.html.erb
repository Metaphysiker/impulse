<header>
  <nav class="navbar navbar-expand-sm navbar-light bg-light">
    <div class="container-fluid">
      <%= link_to root_path, class: "navbar-brand text-h-color" do %>
          Impulse
      <% end %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggler" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarToggler">
        <ul class="navbar-nav mr-auto">
          <% if is_current_user_admin? %>
            <li class="nav-item">
              <%= link_to "Admin-Bereich", overview_admin_index_path, class:"nav-link"  %>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to "Tauschbörse", exchange_path, class:"nav-link"  %>
          </li>
          <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "Mein Impulse", workspace_path, class:"nav-link"  %>
            </li>
            <li class="nav-item">
              <%= link_to "Lebenslauf", cv_generator_my_cvs_path(current_user), class:"nav-link"  %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>
