<% # rails g erb:scaffold Support_request title:string description:text content:text %>

<%# url_cover = support_request.cover.attached? ? rails_blob_url(support_request.cover) : image_url("pigs.jpg") %>
<%# height_of_cover = support_request.cover.attached? ? ActiveStorage::Analyzer::ImageAnalyzer.new(support_request.cover).metadata[:height] : 500 %>


  <section class="page-cover-bg bg b-color">
      <div class="container d-flex align-items-start justify-content-center h-100 pt-2">

          <%#= image_tag("logo_stinah_ws_vektor.png", class: "img-fluid mb-4 rounded overlay-darker", style: "height: 180px;") %>

      </div>
  </section>



<div class="container pt-3">



  <h3 class="text-h-color spaced-letters font-weight-light text-uppercase"><%= support_request.title %></h3>
  <p class="font-weight-bold text-muted pt-3">
    <%#= support_request.description %>
  </p>
  <p>
  <%= simple_format(support_request.content) %>
  </p>
  <% if is_current_user_admin? %>
    <div class="text-center m-2">
      <hr />
      <%= link_to 'Eintrag bearbeiten', edit_support_request_path(support_request), class: "btn btn-primary m-2" %>
      <%= link_to 'Eintrag löschen', support_request_path(support_request), method: :delete, data: {confirm: 'Bist du sicher?'}, class: "btn btn-danger m-2" %>
    </div>
  <% end %>
</div>
