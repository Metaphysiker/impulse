<div class="<%= category %>_cv_units_from_user_<%= user.id %>">
<div class="<%= category %>_cv_unit_list_from_user_<%= user.id %>">
  <% user.cv_units.where(category: category).order(:start_date).each do |cv_unit| %>
    <%= render 'cv_units/cv_unit_li_item', cv_unit: cv_unit %>
  <% end %>
</div>
<% if user == current_user %>
<div class="my-4">
  <%= simple_form_for :cv_unit, url: create_cv_unit_path, method: :post, remote: true do |f| %>
  <p>
    <%= f.input :name, label: false, placeholder: "Erfahrung" %>
    <%= f.input :content, as: :text, label: false, placeholder: "Beschreibung", input_html: { rows: 3 } %>
    <% unless category == "skills" %>
      <%= f.input :start_date, as: :date, label: "Start", required: false %>
      <%= f.input :end_date, as: :date, label: "Ende", required: false %>
    <% end %>
    <%= f.hidden_field :category, value: category %>
    <%= f.submit "Hinzufügen", class: "btn btn-light border my-1" %>
  </p>
  <% end %>
</div>
<% end %>
</div>
