var record_id = ".form_for_user";
var errors = <%= raw @user.errors.full_messages %>;
console.log(errors);

if (jQuery.isEmptyObject(errors)) {
  window.location = "<%= cv_generator_update_user_for_cv_page_path(@user, flash: 'Account wurde erstellt!') %>";

} else {

    content = $("<%= escape_javascript( render 'devise/registrations/new', user: @user ) %>");

    $(record_id).replaceWith(function(){
      return $(content).hide().fadeIn("slow");
    });

}
